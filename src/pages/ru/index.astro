---
import config_json from "../../review_templates/template_en.json";
import PageLayout from "../../layouts/PageLayout.astro";
import OptionForm from "../../components/OptionForm.astro";
import FlagSelect from "../../components/FlagSelect.astro";
---

<PageLayout title="Шаблон обзора Steam">
  <header class="bg-slate-700 text-white py-2 flex flex-col items-center">
    <h1>{"---{ Review Template }---"}</h1>
    <FlagSelect />
    <p class="mt-0">☑ Потрясающе</p>
  </header>

  <div class="max-w-screen-sm mx-auto px-4">
    <p>
      Этот небольшой инструмент упрощает процесс создания обзоров на Steam. Если вы находитесь на этом сайте, то именно его вы ищете. 
      Я нахожу эти обзоры довольно забавными, поэтому создал этот сайт, чтобы избавить себя от необходимости копировать и вставлять так много текста.&nbsp;😆
    </p>
    <p>
      Код с открытым исходным кодом &rarr; <a
        href="https://github.com/VojtaStruhar/steam-review-template"
      >
        проверьте на Github</a
      >!
    </p>
    <p>
      Результат формируется с указанием автора внизу, что является признанием моих заслуг.
      Хотя это не является обязательным, но это очень приятно! Благодарю!
    </p>
    {
      config_json.categories.map((category, index) => (
        <OptionForm category={category} index={index} />
      ))
    }
    <div class="flex justify-center">
      <button
        id="output-button"
        class="py-4 px-8 text-lg font-bold rounded-lg border-none shadow bg-amber-400 hover:cursor-pointer hover:shadow-lg transition-all"
      >
        Создать Отзыв в Steam!</button
      >
    </div>

    <div class="flex justify-center mt-4">
      <button
        id="copy-output"
        class="py-2 px-4 text-lg rounded-lg border-none shadow bg-sky-100 hover:cursor-pointer hover:shadow-lg transition-all"
      >
        Копировать выходные данные</button
      >
    </div>
  </div>
</PageLayout>
